# Set both values as encrypted secrets in the hosting environment.
# SITE_ACCESS_CODE is the human-friendly code shared with invited visitors.
SITE_ACCESS_CODE=
# Use a separate random value of at least 32 characters to sign access cookies.
SITE_ACCESS_SECRET=
